import React from 'react';
import ReactDOM from 'react-dom/client';
import BhabBengaliApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BhabBengaliApp />
  </React.StrictMode>
);
